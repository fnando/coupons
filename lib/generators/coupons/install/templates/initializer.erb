Coupons.configure do |config|
  # Define the token generator. By default it generates a 6 characters
  # alpha-numeric token.
  #
  # The following generator will create a 4 chars alpha-numeric token,
  # appended with the current year.
  # config.generator = -> { "#{SecureRandom.hex[0, 4]}#{Date.today.year}" }

  # Define the coupon resolver.
  # When you create a coupon, you can associate it with any record.
  # This is useful for creating coupons tied to a specific product or category.
  #
  # config.resolvers = [Coupons::Resolver]
end
